import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mine.field.angular',
  appName: 'mine-field-angular',
  webDir: 'docs',
  server: {
    androidScheme: 'https'
  }
};

export default config;
